# Sample Project

A minimal fixture used by the code-index integration tests. It contains a
`Greeter` service, some utility helpers, and non-Dart companion files so the
language-agnostic indexing paths are exercised.

## Usage

Run `runGreeter('world')` to print a greeting.
